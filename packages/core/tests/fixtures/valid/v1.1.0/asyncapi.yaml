asyncapi: 3.0.0
info:
  title: Dummy AsyncAPI
  version: 1.0.0
channels:
  userEvents:
    address: 'user/events'
    messages:
      orderPlaced:
        payload:
          type: object
          properties:
            orderId:
              type: string
operations:
  orderPlaced:
    action: send
    channel:
      $ref: '#/channels/userEvents'
    messages:
      - $ref: '#/channels/userEvents/messages/orderPlaced'
